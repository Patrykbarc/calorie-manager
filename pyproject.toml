[project]
name = "calories-counter"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "fastapi[standard]",
    "pydantic",
    "requests",
    "uvicorn",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]
